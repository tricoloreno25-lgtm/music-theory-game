# Chord Quest ブラッシュアップ：引き継ぎ

最終更新：2026-09-13 JST
作業ディレクトリ：`/Users/yukitajima/music-theory-game`

## 現在の状態

承認済みの7項目の実装と、前回残っていた主要操作のブラウザ確認・画面の仕上げを完了しました。以前このファイルに列挙されていた未完了作業は対応済みです。次回はこの内容を踏まえて、ユーザーの追加指示から進めてください。

ユーザーは2026-09-13に最終確認後のcommit / push / 公開を承認しました。本変更の基点は `36a9626 Add harmony training and rich responsive UI` です。コミットの詳細は `git log`、公開結果はGitHub Actionsの `pages build and deployment` を参照してください。

公開先：<https://tricoloreno25-lgtm.github.io/music-theory-game/>。`origin/main` へのpushで既存のGitHub Pagesが自動更新されます。今回の静的ファイルのキャッシュ識別子は `studio-20260913` です。

日本語で応答してください。同じ確認をむやみに繰り返さず、追加の変更や不具合に関係する確認を優先してください。

## 実装した範囲

1. 濃紺・紫の画面整理、読みやすい成績表示、スマホの1オクターブ鍵盤と音域切り替え。
2. ホームの「今日の5分」「前回の続き」「苦手を3問復習」「音で遊ぶ」、Cメジャー／Cマイナーの聴き比べ。
3. 誤答の具体的な解説、不足音・余分な音、回答とお手本の比較再生。通常練習はヒント・誤答による減点なし。
4. 6段階の学習ルート、教科書の音・鍵盤・小問・練習への導線、耳トレ3段階。
5. 共通音源、音色・音量・テンポ・停止・リピート、和声の小節単位再生、音に連動する表示。
6. 回答履歴による実績、誤答・経過日数・「あとで復習」に基づく復習、単元別の目標。
7. 作曲室の4コード編集、並べ替え、キー・テンポ変更、ループ、名前付きお気に入り保存。

元の11モード・424問、教科書、自由演奏のコード／スケール解析、アカウント別学習履歴を維持しています。

## 今回の再開後に修正した点

- `quizAvailableAtEarLevel()` で通常出題・履歴順の次問・復習候補の耳トレ難易度を統一。履歴から対象外の種類を明示的に開いた場合は、その問題に回答できるステップへ切り替えて通知します。難易度変更時は履歴順出題を解除します。
- 「あとで復習」は `progress.reviewRequestedAt` と即時の復習期限を保存します。未回答を誤答として数えず、得点や挑戦回数は増減しません。保存に失敗したら現在の問題に留まります。
- 復習対象が1〜2問の場合は、同じ難易度で取り組んだ問題や入門問題を補充し、重複なしの3問にします。
- レッスン再開時は、保存済みの回答と正解状態を復元します。完了画面から履歴の問題を開いた場合の表示も修正しました。
- 採点の保存失敗時は得点・挑戦回数を戻し、回答を保持して再試行できます。成功するまでレッスン結果を更新しません。保存中はアカウント切り替え・概念ボードの回答変更等を抑制します。
- IndexedDBのトランザクション中断も失敗として処理。書き込み確定後の集計読み込み失敗では、確定した得点を巻き戻しません。
- `state.navigationId` で遅れた問題・履歴読み込みを無効化。素早い画面移動後に前の問題名が作曲室へ残る不具合を解消しました。
- 和声の回答再生と比較再生も「選択中の1小節」に対応。正解後も小節を選んで聴き直せます。
- 作曲ループはAudioContextの絶対時刻に先行予約し、通常リピート用の80msの間隔を挟みません。停止時は予約済みの音と表示タイマーを解除します。
- スマホはヒントを鍵盤・回答ボタンの後へ移動し、余白を短縮。機能分類・進行の内部に残っていた高さ制限を解消しました。進行カードはスマホで2列、進行ライブラリは各項目を十分な高さで表示します。
- 分類先カード、進行のコード名、作曲室の解析結果・空欄の配色を修正しました。

## ファイル構成と依存関係

- 外部パッケージ不要の静的HTML/CSS/JS。package.json・ビルド工程はありません。
- 読み込み順は **`audio.js` → `app.js` → `experience.js`**。通常のscriptでグローバルスコープを共有します。
- 起動の `init().catch(...)` は `experience.js` 末尾のみ。二重起動させないでください。
- `app.js` は既存機能との接続、`audio.js` は共通音源、`experience.js` はホーム・教材・採点解説・復習・作曲室、`experience.css` は既存CSSの上書きです。
- `styles.css` / `rich-theme.css` / `design-concept.html` は未変更です。
- `index.html` / `app.js` が変更済み。`audio.js` / `experience.js` / `experience.css` / `favicon.svg` / `tests/learning.test.mjs` / `tests/browser.mjs` / このファイルが新規です。

## 保存形式

- IndexedDB名 `chord-quest-db`、バージョン **2を維持**。
- `accounts` / `quizzes` / `progress` / `attemptsLog` と既存424問のIDを維持。既存データの削除はしていません。
- `progress` に `lastResult` / `reviewStreak` / `nextReviewAt` / `earLevel` / `reviewRequestedAt` を追加。次回復習日がない旧レコードは最終正解から3日で復習対象にするフォールバックがあります。
- `attemptsLog` に耳トレの選択・難易度、和声キー、機能の割り当て、ヒント数、取り組み方を追加。
- 設定・途中レッスン・作曲室の下書き／お気に入りは `localStorage` の **`chordQuest:experience:${accountId}`**。既存の `chordQuest:lastAccountId` も維持。
- 保存範囲は同じブラウザ・同じオリジンのユーザーごと。クラウド同期は対象外です。

## 実施した確認

### 構文・単体

`node --check app.js` / `node --check audio.js` / `node --check experience.js` / `node --check tests/browser.mjs` と `node --test tests/learning.test.mjs` が成功。単体テストは11件すべて成功です。耳トレ候補と明示的な復習予約の検証も追加済みです。`git diff --check` も成功しています。

### ブラウザ

依存追加なしの `tests/browser.mjs` でChrome DevTools Protocolを使用。毎回独立した一時ブラウザコンテキストを作り、終了時に破棄します。通常のユーザー領域の履歴・設定は変更しません。

以下の主要操作を確認済みです。

- 今日の5分：回答→ホーム→回答状態を保って再開→5問完了。
- コード：誤答→具体的解説→比較再生→選び直して正解。練習時のヒント・誤答に減点なし。
- 耳トレ：誤答後の再選択、履歴順の次問、augmentedの履歴を開いた場合のステップ切替、難易度変更。
- スキップ：復習予約が保存され、挑戦回数は増えず、復習セッションは3問。
- 教科書：デモ→小問→対応する練習。
- 作曲室：小節選択、コード変更、左右移動、キー・テンポ変更、ループ、停止、保存、再読み込みしてお気に入りを復元。
- 160 BPMのループ境界を含む音の予約時刻が1小節1.5秒間隔。停止後の音源・表示タイマー・次回予約がゼロ。
- ユーザー新規作成と切り替え：履歴・設定・途中レッスン・お気に入りが混ざらない。
- 1小節／4小節の和声、機能、進行、ダイアトニックをクリック操作で正解。和声の選択中小節の回答再生。
- スマホの音域切替を使ってGメジャースケールの回答を保持・採点。
- 保存失敗の注入：回答・得点を保持、アカウント変更を抑制、再試行で1回だけ記録。
- 問題取得と履歴読み込みを意図的に遅らせ、後から移動した画面を書き換えないこと。
- ホーム・問題・作曲室等を1440px／390pxで撮影・目視。修正した機能・進行は両幅で確認。全進行問題のスマホでの横はみ出し・カード内の欠けを確認。
- 作曲室の自由鍵盤でC・E・Gを押し、Cメジャーの一致解析を確認。

ブラウザ確認は主要経路の統合確認です。全424問をクリックで解いたという意味ではありません。

## 再確認が必要になった場合の起動手順

```sh
python3 -m http.server 8765 --bind 127.0.0.1
```

専用Chromeをポート9337で起動してから：

```sh
node tests/browser.mjs
# 見た目の修正箇所だけ確認する場合
node tests/browser.mjs /private/tmp/chord-quest-browser-review visual
```

`http://127.0.0.1:8765` を使用します。キャッシュを無視して再読み込みしてください。Chromeの起動・ローカル接続は環境によって正規の権限昇格が必要です。

画面画像は `/private/tmp/chord-quest-browser-review/` にあります。前回の `/private/tmp/chord-quest-review.8RkL5N/` の画像は旧状態なので、最新の目視確認には使わないでください。

今回起動したHTTPサーバーと確認専用Chromeは作業終了時に停止済みです。通常のユーザーブラウザには停止操作をしていません。

## 制約・未実施

- 音色はWeb Audioの倍音合成であり、録音ピアノサンプルではありません。発音予約・停止・ループ周期は確認済みですが、実スピーカーでの聴感評価は未実施です。
- スマホはChromeの390pxエミュレーション。iPhone/SafariやAndroid実機での確認は未実施です。
